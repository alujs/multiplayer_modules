var Rooms = require("../lib/manager.js");


describe("Manager", function () {

  xit("It should not overwrite old data ", function () {
    room_list = new Rooms(socketid, 'dolem', init_data);

  });

});  